---
const links = [
  { href: '/estudio', label: 'estudio' },
  { href: '/styling', label: 'styling' },
  { href: '/proyectos', label: 'proyectos' },
  { href: '/muestras', label: 'muestras' },
  { href: '/shop', label: 'shop' },
  { href: '/contacto', label: 'contacto' },
];
---
<nav class="w-full fixed top-0 left-0 z-50">
  <div class="container flex items-center justify-between py-5">
    <a href="/" class="tracking-tightish font-serif text-xl">GALERÍA</a>
    <ul class="hidden md:flex gap-6 text-sm">
      {links.map((l) => (
        <li><a class="hover:opacity-70" href={l.href}>{l.label}</a></li>
      ))}
    </ul>
  </div>
</nav>
