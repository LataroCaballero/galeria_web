---
const { src = '/images/hero.jpg', alt = 'Hero', children } = Astro.props;
---
<section id="top" class="relative">
  <img src={src} alt={alt} class="w-full h-[70svh] md:h-[85svh] object-cover" loading="eager" />
  <div class="absolute inset-0 bg-gradient-to-t from-black/10 to-transparent"></div>
  <div class="absolute inset-x-0 bottom-6 container text-white">
    <h1 class="font-serif text-2xl md:text-3xl tracking-tightish">GALERÍA</h1>
  </div>
</section>
