---
const { src, alt, title } = Astro.props;
---
<figure class="space-y-3">
  <img src={src} alt={alt} class="w-full aspect-[3/4] object-cover" loading="lazy" />
  <figcaption class="text-xs uppercase tracking-[0.12em]">{title}</figcaption>
</figure>